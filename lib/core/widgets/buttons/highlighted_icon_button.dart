import 'package:flutter/material.dart';

class HighlightedIconButton extends StatelessWidget {
  const HighlightedIconButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
